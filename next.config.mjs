/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.freepik.com','books.google.com'],// Add other domains as needed
      },
      output: 'export',
};

export default nextConfig;
